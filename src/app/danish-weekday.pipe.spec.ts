import { DanishWeekdayPipe } from './danish-weekday.pipe';

describe('DanishWeekdayPipe', () => {
  it('create an instance', () => {
    const pipe = new DanishWeekdayPipe();
    expect(pipe).toBeTruthy();
  });
});
